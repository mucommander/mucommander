repositories.mavenCentral()

test.useTestNG()

dependencies {
    api project(':barebones-commons-io')
    api project(':barebones-commons-runtime')
    api project(':barebones-commons-util')

    comprise 'net.java.dev.jna:jna:5.5.0'
    comprise 'net.java.dev.jna:jna-platform:5.5.0'
    implementation 'org.apache.commons:commons-collections4:4.5.0'
    implementation 'org.apache.commons:commons-lang3:3.20.0'

    implementation("com.ibm.icu:icu4j:78.3")

    testImplementation 'org.testng:testng:7.11.0'
    testImplementation(platform('org.junit:junit-bom:5.14.1'))
    testImplementation('org.junit.jupiter:junit-jupiter')
    testRuntimeOnly('org.junit.platform:junit-platform-launcher')
    testImplementation project(':barebones-format-zip')
}

jar {
    from configurations.comprise.collect { it.isDirectory() ? it : zipTree(it).matching {
        exclude 'com/sun/jna/freebsd-x86/**'
        exclude 'com/sun/jna/freebsd-x86-64/**'
        exclude 'com/sun/jna/linux-arm/**'
        exclude 'com/sun/jna/linux-x86/**'
        exclude 'com/sun/jna/linux-x86-64/**'
        exclude 'com/sun/jna/openbsd-x86/**'
        exclude 'com/sun/jna/openbsd-x86-64/**'
        exclude 'com/sun/jna/sunos-sparc/**'
        exclude 'com/sun/jna/sunos-sparcv9/**'
        exclude 'com/sun/jna/sunos-x86/**'
        exclude 'com/sun/jna/sunos-x86-64/**'
        exclude 'com/sun/jna/w32ce-arm/**'
        exclude 'com/sun/jna/darwin/**'
        exclude 'META-INF/**'
    } }

    bundle {
        bnd ('Bundle-Name': 'barebones-commander-commons-file',
                'Bundle-Vendor': 'barebones-commander',
                'Bundle-Description': 'Component with file-level utilities',
                'Bundle-DocURL': 'https://github.com/e6qu/barebones-commander',
                'Bundle-Activator': 'dev.barebones.commander.commons.file.osgi.Activator',
                'Export-Package':
                        'dev.barebones.commander.commons.file,' +
                                'dev.barebones.commander.commons.file.archive,' +
                                'dev.barebones.commander.commons.file.connection,' +
                                'dev.barebones.commander.commons.file.filter,' +
                                'dev.barebones.commander.commons.file.icon,' +
                                'dev.barebones.commander.commons.file.icon.impl,' +
                                'dev.barebones.commander.commons.file.osgi,' +
                                'dev.barebones.commander.commons.file.protocol,' +
                                'dev.barebones.commander.commons.file.protocol.local,' +
                                'dev.barebones.commander.commons.file.protocol.search,' +
                                'dev.barebones.commander.commons.file.util,' +
                                'com.sun.jna.platform.win32',
                'Specification-Title': "barebones-commander",
                'Specification-Vendor': "barebones-commander contributors",
                'Specification-Version': project.version,
                'Implementation-Title': "barebones-commander",
                'Implementation-Vendor': "barebones-commander contributors",
                'Implementation-Version': revision,
                'Build-Date': new Date().format('yyyyMMdd'))
    }
}

test {
    useTestNG()
    testLogging {
        events "passed", "skipped", "failed"
    }
}
